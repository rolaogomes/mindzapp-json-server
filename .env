# Porta já usada pelo teu servidor
PORT=4321
PUBLIC_BASE_URL=http://localhost:4321

# Remetente
FROM_EMAIL="MindZapp <no-reply@mindzapp.app>"

# SMTP real (ex.: Mailtrap SMTP, SendGrid SMTP, etc.)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
